# Root .env is used by system tests (bun run test:system).
# Package-specific variables go in each package's own .env file.
DB_CONNECTION_URL=postgresql://tester:example@localhost/default
REDIS_URL=redis://localhost:6379
