{
  "name": "xinity-infoserver",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "refresh-schema": "bun run definitions/model-definition.ts",
    "dev": "bun run server.ts | pino-pretty",
    "build:compile": "bun build --compile --minify --target=bun ./server.ts --outfile xinity-infoserver",
    "test": "bun test ."
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "pino-pretty": "catalog:"
  },
  "peerDependencies": {
    "typescript": "catalog:"
  },
  "dependencies": {
    "common-env": "workspace:*",
    "common-log": "workspace:*",
    "zod": "catalog:"
  }
}
