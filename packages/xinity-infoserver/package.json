{
  "name": "xinity-infoserver",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "refresh-schema": "bun run definitions/model-definition.ts > models.schema.json",
    "dev": "bun run server.ts | pino-pretty",
    "build:compile": "bun build --compile --minify --target=bun ./server.ts --outfile xinity-infoserver",
    "test": "bun test src/"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "pino-pretty": "^13.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "common-env": "workspace:*",
    "common-log": "workspace:*",
    "zod": "^4.2.1"
  }
}
